use crate::{Building, RecipeCSV};
use prettytable::{Cell, Row, Table};
use std::collections::HashMap;

fn list_needed_tables(
    chosen_recipe: &String,
    recipes: &HashMap<String, RecipeCSV>,
    buildings: &HashMap<String, Building>,
) {
}
